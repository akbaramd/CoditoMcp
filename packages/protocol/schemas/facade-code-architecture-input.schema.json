{
  "$id": "https://codito.akbaramd.ir/schemas/v1/facade-code-architecture-input.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "max_packages": {
      "default": 100,
      "description": "Maximum package/layer nodes returned.",
      "maximum": 500,
      "minimum": 1,
      "title": "Max Packages",
      "type": "integer"
    },
    "project_id": {
      "description": "Opaque project ID returned by projects_list; never send a local path.",
      "title": "Project Id",
      "type": "string"
    },
    "purpose": {
      "description": "Concise user-facing reason shown in Windows approval and local activity history.",
      "maxLength": 1000,
      "minLength": 1,
      "title": "Purpose",
      "type": "string"
    }
  },
  "required": [
    "project_id",
    "purpose"
  ],
  "title": "CodeArchitectureFacadeInput",
  "type": "object"
}
