{
  "$id": "https://codito.akbaramd.ir/schemas/v1/facade-code-dependencies-input.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "direction": {
      "default": "direct",
      "description": "Return direct, transitive, or all known dependencies.",
      "enum": [
        "all",
        "direct",
        "transitive"
      ],
      "title": "Direction",
      "type": "string"
    },
    "max_results": {
      "default": 100,
      "description": "Maximum dependency records returned.",
      "maximum": 500,
      "minimum": 1,
      "title": "Max Results",
      "type": "integer"
    },
    "path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional project-relative manifest, package, or source path.",
      "title": "Path"
    },
    "project_id": {
      "description": "Opaque project ID returned by projects_list; never send a local path.",
      "title": "Project Id",
      "type": "string"
    },
    "purpose": {
      "description": "Concise user-facing reason shown in Windows approval and local activity history.",
      "maxLength": 1000,
      "minLength": 1,
      "title": "Purpose",
      "type": "string"
    }
  },
  "required": [
    "project_id",
    "purpose"
  ],
  "title": "CodeDependenciesFacadeInput",
  "type": "object"
}
