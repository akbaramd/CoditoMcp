{
  "$id": "https://codito.akbaramd.ir/schemas/v1/facade-file-delete-input.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "base_hash": {
      "description": "Current SHA-256 returned by file_read.",
      "title": "Base Hash",
      "type": "string"
    },
    "dry_run": {
      "default": false,
      "description": "Validate the deletion without changing the file.",
      "title": "Dry Run",
      "type": "boolean"
    },
    "idempotency_key": {
      "description": "Caller-generated stable unique key for this logical mutation or command. Reuse only when retrying that exact same request.",
      "title": "Idempotency Key",
      "type": "string"
    },
    "path": {
      "description": "Exact single file to delete, relative to the target root.",
      "title": "Path",
      "type": "string"
    },
    "project_id": {
      "description": "Opaque project ID returned by projects_list; never send a local path.",
      "title": "Project Id",
      "type": "string"
    },
    "purpose": {
      "description": "Concise user-facing reason shown in Windows approval and local activity history.",
      "maxLength": 1000,
      "minLength": 1,
      "title": "Purpose",
      "type": "string"
    },
    "scope_path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional requested absolute Windows directory outside the project, e.g. C:/Temp. Local project policy and Windows approval still apply; not a trust assertion.",
      "title": "Scope Path"
    }
  },
  "required": [
    "project_id",
    "purpose",
    "path",
    "base_hash",
    "idempotency_key"
  ],
  "title": "FileDeleteInput",
  "type": "object"
}
