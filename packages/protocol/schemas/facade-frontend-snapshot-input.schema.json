{
  "$id": "https://codito.akbaramd.ir/schemas/v1/facade-frontend-snapshot-input.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "max_elements": {
      "default": 500,
      "description": "Maximum semantic elements returned with the viewport PNG.",
      "maximum": 500,
      "minimum": 1,
      "title": "Max Elements",
      "type": "integer"
    },
    "project_id": {
      "description": "Opaque project ID returned by projects_list; never send a local path.",
      "title": "Project Id",
      "type": "string"
    },
    "purpose": {
      "description": "Concise user-facing reason shown in Windows approval and local activity history.",
      "maxLength": 1000,
      "minLength": 1,
      "title": "Purpose",
      "type": "string"
    },
    "session_id": {
      "description": "Frontend session ID returned by frontend_session_start.",
      "title": "Session Id",
      "type": "string"
    }
  },
  "required": [
    "project_id",
    "session_id",
    "purpose"
  ],
  "title": "FrontendSnapshotFacadeInput",
  "type": "object"
}
