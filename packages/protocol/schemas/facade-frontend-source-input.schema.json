{
  "$id": "https://codito.akbaramd.ir/schemas/v1/facade-frontend-source-input.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "element_id": {
      "description": "Semantic element ID whose project-relative source locations are requested.",
      "pattern": "^e[1-9][0-9]{0,7}$",
      "title": "Element Id",
      "type": "string"
    },
    "project_id": {
      "description": "Opaque project ID returned by projects_list; never send a local path.",
      "title": "Project Id",
      "type": "string"
    },
    "purpose": {
      "description": "Concise user-facing reason shown in Windows approval and local activity history.",
      "maxLength": 1000,
      "minLength": 1,
      "title": "Purpose",
      "type": "string"
    },
    "session_id": {
      "description": "Frontend session ID returned by frontend_session_start.",
      "title": "Session Id",
      "type": "string"
    },
    "snapshot_id": {
      "description": "Snapshot ID that minted element_id; stale IDs fail.",
      "title": "Snapshot Id",
      "type": "string"
    }
  },
  "required": [
    "project_id",
    "session_id",
    "purpose",
    "snapshot_id",
    "element_id"
  ],
  "title": "FrontendSourceFacadeInput",
  "type": "object"
}
