{
  "$id": "https://codito.akbaramd.ir/schemas/v1/facade-shell-status-input.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "job_id": {
      "description": "Job ID returned by execute_shell.",
      "title": "Job Id",
      "type": "string"
    },
    "max_output_bytes": {
      "default": 262144,
      "description": "Maximum output bytes in one response page. Continue from the returned cursor while has_more_output is true.",
      "maximum": 1048576,
      "minimum": 16384,
      "title": "Max Output Bytes",
      "type": "integer"
    },
    "project_id": {
      "description": "Opaque project ID returned by projects_list; never send a local path.",
      "title": "Project Id",
      "type": "string"
    },
    "sequence_cursor": {
      "default": 0,
      "description": "Next output sequence cursor returned by shell_status.",
      "minimum": 0,
      "title": "Sequence Cursor",
      "type": "integer"
    },
    "wait_milliseconds": {
      "default": 30000,
      "description": "Maximum long-poll wait for new output or a terminal state.",
      "maximum": 30000,
      "minimum": 0,
      "title": "Wait Milliseconds",
      "type": "integer"
    }
  },
  "required": [
    "project_id",
    "job_id"
  ],
  "title": "ShellStatusInput",
  "type": "object"
}
